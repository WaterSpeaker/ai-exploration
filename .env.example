# Site audience: external (default, public) | internal (intranet)
PUBLIC_SITE_AUDIENCE=external

# External-only soft access password (client-side gate; default 1234)
PUBLIC_SITE_ACCESS_PASSWORD=1234

# Optional override for the Contact me pill (defaults to Lark add-contact invite)
PUBLIC_LARK_PROFILE_URL=
